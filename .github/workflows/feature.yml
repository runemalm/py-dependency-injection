name: Push on feature

on:
  push:
    branches:
      - feature*

jobs:
  unittests:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run unittests
        uses: ./.github/actions/unittests
